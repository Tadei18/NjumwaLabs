Drop real images here.

- crispus-njumwa.jpg  → professional headshot for the About profile card.
  Recommended: square, ~600×600+, neutral or dark background.
  Until it exists, the card shows a "CN" monogram fallback automatically
  (the <img> removes itself on 404 — no broken-image icon).

- Project screenshots / UI mockups for case studies can also live here and be
  referenced via the `cover` field in src/content/portfolio/*.mdx.
