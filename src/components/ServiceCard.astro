---
import { Icon } from "astro-icon/components";
import type { Pillar } from "@data/services";

interface Props {
  pillar: Pillar;
  index?: number;
}
const { pillar, index } = Astro.props;
---

<a href={pillar.href} class="service-card card" data-reveal>
  {index !== undefined && (
    <>
      <span class="card-num">{String(index + 1).padStart(2, "0")}</span>
      <span class="card-watermark" aria-hidden="true">{String(index + 1).padStart(2, "0")}</span>
    </>
  )}
  <span class="card-icon">
    <Icon name={`lucide:${pillar.icon}`} />
  </span>
  <h3 class="card-title">{pillar.title}</h3>
  <p class="card-outcome">{pillar.outcome}</p>
  <span class="card-cta">
    Explore
    <Icon name="lucide:arrow-right" />
  </span>
</a>

<style>
  .service-card {
    position: relative;
    display: flex;
    flex-direction: column;
    padding: 1.6rem;
    overflow: hidden;
    transition: transform 0.22s var(--ease-out), box-shadow 0.22s var(--ease-out),
      border-color 0.22s var(--ease-out);
  }
  /* accent top-bar that sweeps in on hover */
  .service-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--color-accent), var(--color-accent-2));
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s var(--ease-out);
  }
  .service-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lift);
    border-color: color-mix(in srgb, var(--color-accent) 50%, var(--color-line));
  }
  .service-card:hover::before {
    transform: scaleX(1);
  }
  .card-watermark {
    position: absolute;
    bottom: -1.5rem;
    right: -0.5rem;
    font-family: var(--font-mono);
    font-weight: 700;
    font-size: 7rem;
    line-height: 1;
    color: var(--color-surface-2);
    z-index: 0;
    pointer-events: none;
    transition: color 0.3s var(--ease-out);
  }
  .service-card:hover .card-watermark {
    color: color-mix(in srgb, var(--color-accent) 9%, var(--color-surface-2));
  }
  .card-num {
    position: absolute;
    top: 1.2rem;
    right: 1.4rem;
    z-index: 1;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--color-muted);
  }
  .card-icon,
  .card-title,
  .card-outcome,
  .card-cta {
    position: relative;
    z-index: 1;
  }
  .card-icon {
    display: inline-flex;
    width: 3rem;
    height: 3rem;
    align-items: center;
    justify-content: center;
    border-radius: 14px;
    background: color-mix(in srgb, var(--color-accent) 12%, var(--color-surface));
    color: var(--color-accent-ink);
    margin-bottom: 1.1rem;
  }
  .card-icon svg {
    width: 1.5rem;
    height: 1.5rem;
  }
  .card-title {
    font-size: 1.2rem;
  }
  .card-outcome {
    margin-top: 0.5rem;
    color: var(--color-muted);
    line-height: 1.55;
    flex-grow: 1;
  }
  .card-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    margin-top: 1.25rem;
    font-family: var(--font-display);
    font-weight: 500;
    font-size: 0.9rem;
    color: var(--color-accent-ink);
  }
  .card-cta svg {
    width: 1rem;
    height: 1rem;
    transition: transform 0.2s var(--ease-out);
  }
  .service-card:hover .card-cta svg {
    transform: translateX(4px);
  }
</style>
