---
// A figure with an inline source attribution — builds the sourced, citable feel
// AI engines favour. Usable in .astro and MDX.
interface Props {
  value: string;
  label: string;
  source?: string;
  sourceUrl?: string;
}
const { value, label, source, sourceUrl } = Astro.props;
---

<div class="stat-callout">
  <span class="sc-value">{value}</span>
  <span class="sc-label">{label}</span>
  {source && (
    <span class="sc-source">
      Source:
      {sourceUrl ? (
        <a href={sourceUrl} target="_blank" rel="noopener">{source}</a>
      ) : (
        <span>{source}</span>
      )}
    </span>
  )}
</div>

<style>
  .stat-callout {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    padding: 1.25rem 1.5rem;
    margin: 1.5rem 0;
    background:
      linear-gradient(165deg, rgba(255, 255, 255, 0.55), rgba(255, 255, 255, 0) 60%),
      var(--color-surface);
    border: 1px solid var(--glass-edge);
    border-left: 3px solid var(--color-accent);
    border-radius: 12px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.85),
      0 6px 20px rgba(16, 24, 43, 0.05);
  }
  .sc-value {
    font-family: var(--font-mono);
    font-weight: 700;
    font-size: clamp(1.8rem, 4vw, 2.4rem);
    line-height: 1;
    color: var(--color-accent-ink);
  }
  .sc-label {
    margin-top: 0.3rem;
    color: var(--color-ink-soft);
    line-height: 1.4;
  }
  .sc-source {
    margin-top: 0.5rem;
    font-size: 0.78rem;
    color: var(--color-muted);
  }
  .sc-source a {
    color: var(--color-accent-ink);
    text-decoration: underline;
  }
</style>
